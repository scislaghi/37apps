/**
 * AdMob ad unit IDs for Hue (bundle com.simonecislaghi.hue).
 *
 * Left empty until the game is registered as its own app inside the shared
 * 37apps AdMob account — `@37apps/core/ads.js` falls back to Google's public
 * test unit IDs for anything missing here, so the integration is live and
 * safe to run right now. Fill these in at publish time (and put the App IDs
 * in strings.xml / Info.plist, not here); no other code changes needed.
 */
export const AD_IDS = {
  android: {},
  ios: {},
};
