/**
 * Real per-platform AdMob ad unit IDs go here once Hopper is registered as
 * its own app inside the shared 37apps AdMob account. Left empty for now —
 * `@37apps/core/ads.js` falls back to Google's public test ad units.
 */
export const AD_IDS = {};
