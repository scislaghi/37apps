import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simonecislaghi.prometheus',
  appName: 'Prometheus',
  webDir: 'dist'
};

export default config;
