import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simonecislaghi.ploopclimb',
  appName: 'Ploop Climb',
  webDir: 'dist'
};

export default config;
