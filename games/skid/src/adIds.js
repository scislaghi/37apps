/**
 * AdMob ad unit IDs for Skid (bundle com.simonecislaghi.skid).
 *
 * Empty on purpose: Skid isn't registered as its own app in the shared 37apps
 * AdMob account yet, so `@37apps/core/ads.js` falls back to Google's public
 * test unit IDs — which show placeholder creatives and, unlike another game's
 * real units, can't earn this bundle an invalid-traffic strike. Fill both
 * platforms in once the app exists in AdMob; nothing else has to change.
 */
export const AD_IDS = {};
